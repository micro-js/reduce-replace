{
  "name": "@micro-js/reduce-replace",
  "description": "A reducer that replaces the accumulator with the next value",
  "repository": "git://github.com/micro-js/reduce-replace.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/identity": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
